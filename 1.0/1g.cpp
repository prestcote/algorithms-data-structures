#include <iostream>

int main() {
  int n = 0;
  int k = 0;
  int m = 0;
  int count = 0;
  std::cin >> n >> k >> m;
  //count += (n / k) * (k / m);
  //int ost = (n % k) + ((n / k) * (k % m));
  //if (ost >)
  while (n >= k) {
    //std::cout << "YAY";
    //count += (n / k) * (k / m);
    //n = (n % k) + ((n / k) * (k % m));
    //n -= 
  }
  std::cout << count;
}